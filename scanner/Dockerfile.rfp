FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY rfp_scanner.py .

ENV RFP_DB=/data/rfp_scanner.db
ENV RFP_SCAN_INTERVAL=4

EXPOSE 8421

VOLUME /data

CMD ["python", "rfp_scanner.py", "--serve", "--port", "8421"]
